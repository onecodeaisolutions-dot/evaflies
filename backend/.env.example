# Copie para .env e preencha. NUNCA comite o .env com a chave real.

# Obrigatória: sua chave da OpenAI
OPENAI_API_KEY=sk-...

# Porta do servidor
PORT=3000

# Modelo de transcrição: gpt-4o-transcribe | gpt-4o-mini-transcribe | whisper-1
TRANSCRIBE_MODEL=gpt-4o-transcribe

# Modelo usado para gerar resumo / action items
SUMMARY_MODEL=gpt-4o-mini

# Idioma esperado da fala (ISO-639-1). Ex: pt, en, es
TRANSCRIBE_LANGUAGE=pt

# Origem permitida para CORS (a extensão usa origem chrome-extension://...).
# Deixe * para permitir qualquer origem em desenvolvimento.
CORS_ORIGIN=*

# --- Armazenamento persistente (Supabase) ---------------------------------
# Se preencher SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, as reuniões vão para o
# Postgres e os áudios para o Storage do Supabase. Se deixar em branco, o
# backend usa arquivos locais (DATA_DIR) — bom só para desenvolvimento.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_BUCKET=meeting-audio

# Diretório local de dados (modo arquivo, quando NÃO usar Supabase).
# DATA_DIR=

# --- Separação por vendedor (autenticação por código de acesso) ------------
# Lista de usuários em JSON. Cada um tem "key" (código de acesso secreto) e
# "name". "admin": true vê TODAS as reuniões; os demais veem só as suas.
# Se ficar vazio/ausente, a autenticação fica DESLIGADA (tudo aberto).
# Exemplo:
# EVA_USERS=[{"key":"rian-9f3a2b","name":"Rian","admin":true},{"key":"vend1-7c2b8d","name":"Vendedor 1"},{"key":"vend2-4d8e1f","name":"Vendedor 2"}]
EVA_USERS=
